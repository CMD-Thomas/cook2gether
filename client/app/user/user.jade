template(name="user")
	h4 #{user.username}'s profile
	+ingredients
	
template(name="ingredients")
	if params
		+ingredients_form
	p I have the following ingredients:
	ul 
		each ing
			if isCreatedby
				li #{name} #[a(href="#" class="deleteIng") X]
			else 
				li #{name}
	+cook

template(name="ingredients_form")
	form  
		input(type="text" placeholder="Ingrediënt" id="ingredientInput")
		button(type="submit") Voeg toe


template(name="cook")
	if params
		p Add ingredients with the form!
	else 
		a(href="/chat/#{user._id}" id="#userId").btn.btn-primary.cook2gether Cook2gether